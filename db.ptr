$TTL    604800
@       IN      SOA     helper.<basedomain>. admin.<basedomain>. (
                  6     ; Serial
             604800     ; Refresh
              86400     ; Retry
            2419200     ; Expire
             604800     ; Negative Cache TTL
)

; name servers - NS records
    IN      NS      helper.<base-domain>.

; name servers - PTR records
<angka-ip-blakang-helper>    IN    PTR    helper.<base-domain>.

; OpenShift Container Platform cluster - PTR records
<angka-ip-blakang>    IN    PTR    bootstrap.<cluster-name>.<base-domain>. 
<>    IN    PTR    bastion.<cluster-name>.<base-domain>. 
<>    IN    PTR    <master-name>.<cluster-name>.<base-domain>. 
<>    IN    PTR    <master-name>.<cluster-name>.<base-domain>. 
<>    IN    PTR    <master-name>.<cluster-name>.<base-domain>. 
<>    IN    PTR    <worker-name>.<cluster-name>.<base-domain>. 
<>    IN    PTR    <worker-name>.<cluster-name>.<base-domain>. 
<>    IN    PTR    <worker-name>.<cluster-name>.<base-domain>. 
<>    IN    PTR    <infra-name>.<cluster-name>.<base-domain>. 
<>    IN    PTR    <infra-name>.<cluster-name>.<base-domain>. 
<>    IN    PTR    <infra-name>.<cluster-name>.<base-domain>. 
<ip-blakang-helper/lb>    IN    PTR    api.<cluster-name>.<base-domain>. 
<>    IN    PTR    api-int.<cluster-name>.<base-domain>.
